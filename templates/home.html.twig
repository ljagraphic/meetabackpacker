{% extends "layouthome.html.twig" %}

{% block body %}
    <style>
        #map {
            height: 400px;
            width: 100%;
        }
    </style>
    <section class="clearfix p0">
        <body id="body" class="body-wrapper boxed-menu" >

            <div id="map"></div>

        </body>
    </section>
    <script>


        var geocoder;
        var map;
        var pub = 'img/marker/drink.png';
        var restaurant = 'img/marker/food.png';
                   /* curiosity {
                    icon: 'img/marker/curiosity.png'
                    },
                    pub/nigthclub: {
                    icon: 'img/marker/drink.png'
                    }
                    eco tourism: {
                    icon: 'img/marker/eco.png'
                    }
                    culture: {
                    icon: 'img/marker/culture.png'
                    }
                    restaurant: {
                    icon: 'img/marker/restaurant.png'
                    }
                    shopping: {
                    icon: 'img/marker/shopping.png'
                    }
                    outdoor activity: {
                    icon: 'img/marker/tourist.png'
                    }
                    }; */
        
            googlePush(function () {
                var angers = {lat: 47.478419, lng: -0.5631660000000238};
                map = new google.maps.Map(document.getElementById('map'), {
                    zoom: 4,
                    center: angers,
                });
                ;
                {% for marker in markers %}
                    new google.maps.Marker({
                        icon: {{marker.category}},
                        position: {
                            lat: {{marker.latitude}},
                            lng: {{marker.longitude}}
                        },
                        map: map
                       
                    });
                   
                {% endfor %}
                geocoder = new google.maps.Geocoder();
                var latlng = new google.maps.LatLng(48.864716, 2.349014);
                var mapOptions = {
                    zoom: 8,
                    center: latlng
                };
            });
    </script>




{% endblock %}